# 시스템 아키텍처 및 데이터 흐름

## 1. 서버 권위적 모델 (Server Authoritative)
모든 게임 내 수치 변동은 서버의 승인이 필요합니다.

### 🔄 데이터 업데이트 흐름
1. **이벤트 발생**: 클라이언트에서 몬스터 처치 또는 아이템 사용.
2. **검증 요청**: 클라이언트가 서버 API(`POST`) 호출.
3. **서버 검증**: 
   - 유저의 현재 상태와 요청 내용의 정합성 확인.
   - DB 트랜잭션을 통해 수치 업데이트.
4. **결과 수신**: 서버로부터 최신화된 데이터(JSON)를 받아 클라이언트 UI 갱신.



## 2. 클라이언트 예측 및 동기화 (Client Prediction)
- 네트워크 지연을 줄이기 위해, 비주얼적인 효과(애니메이션, 이펙트)는 즉시 실행됩니다.
- 실제 수치(골드, 경험치)는 서버 응답 후에 최종 확정됩니다.
